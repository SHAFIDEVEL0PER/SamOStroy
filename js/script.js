/**
 * @file Основной скрипт для веб-приложения.
 * @author Ваше Имя
 * @version 1.0.0
 */

/**
 * Вычисляет сумму двух чисел.
 * @param {number} a - Первое число.
 * @param {number} b - Второе число.
 * @returns {number} Сумма a и b.
 */
function sum(a, b) {
    return a + b;
}